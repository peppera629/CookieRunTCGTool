package app;

/**
 * Marker/anchor class used to locate the application home directory (jar folder / classes folder).
 * Keep this class in the main application artifact.
 */
public final class AppAnchor {
    private AppAnchor() {}
}